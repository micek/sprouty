# Sprouty Python LiveKit voice agent dependencies.
#
# Pins are minimum-known-good versions — bump alongside `livekit-agents` when
# upgrading. The agent runs on Python 3.11+; CI / Cloud deploy targets 3.12.
#
# Install:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Run (dev):
#   python agent.py dev

livekit-agents>=0.11.0
livekit-plugins-openai>=0.10.0
livekit-plugins-silero>=0.7.0
python-dotenv>=1.0.1

# Mid-conversation knowledge-base lookups. The agent embeds the user's
# question via OpenRouter (already wired) and queries Qdrant directly.
qdrant-client>=1.10.0
